{
  "version": 2,
  "buildCommand": "cp backend/prisma/schema.prod.prisma backend/prisma/schema.prisma && cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install && npx prisma generate",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "backend/prisma/schema.prisma"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/((?!assets/|.*\\.).*)", "destination": "/index.html" }
  ]
}
